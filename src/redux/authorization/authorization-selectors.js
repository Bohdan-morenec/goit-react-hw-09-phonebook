export const getUserInformation = (state) => state.authorization.authorization;

export const userName = (state) => state.authorization.user.name;

export const loggerAuthorization = (state) => state.authorization.logger;
